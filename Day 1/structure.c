// C lang executes line by line in linear fashion (Top to bottom).

#include <stdio.h> // header file library

// This is main function of program
int main(){
    // Your Code Here

    // This returns 0 when program has no errors (SUCCESS = 0).
    return 0;
}